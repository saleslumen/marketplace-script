/**
 * @description Alias of initiateConsentsForMailboxes (legacy name).
 * @param {Object} input
 * @returns {Object}
 */
async function initiateConsentForMailboxes(input) {
  return initiateConsentsForMailboxes(input);
}
